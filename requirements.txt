# For running tests with pytest
pytest==6.2.5

# For handling CSV file operations (if needed)
pandas==1.3.3

# For logging (if you use a custom logging library)
loguru==0.5.3

# For working with timestamps or datetime manipulations (optional, depending on your code)
datetime==4.3

# If you use any other web framework (e.g., Flask or Django)
# Flask==2.0.1  # Uncomment if using Flask

setuptools>=51.0.0
